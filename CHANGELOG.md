# Changelog

All notable changes to **AletheiaCLI** are documented here.

Format and release sections follow the project Changelog Protocol (local agent rules under `.agent/`).

## [Unreleased]

### Planned
- Phase 1.1: Pydantic envelope (`ok`, `data`, `meta`, `errors`) and `trace_id`
- Phase 1.2–1.4: stdin ingress, `health`, `read_log` pipeline